# =============================================================================
# requirements.txt — vLLM 推理性能实验依赖（版本锁定）
# =============================================================================

# 推理框架
vllm>=0.6.0,<0.7.0

# HTTP 客户端 (压测和服务验证)
aiohttp>=3.9.0
requests>=2.31.0

# 数据处理与结果分析
numpy>=1.26.0
pandas>=2.1.0

# GPU 监控
pynvml>=11.5.0

# 进度显示
tqdm>=4.66.0
